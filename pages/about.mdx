export const meta = { title: 'About' }

# This is an About page, written with MDX

And this text is entirely styled with `@tailwindcss/typography`

import ProseLayout from '@layouts/prose'
export default ProseLayout
